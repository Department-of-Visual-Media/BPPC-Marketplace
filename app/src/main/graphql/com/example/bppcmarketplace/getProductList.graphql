query getProductList($page:Int!){
    products(page: $page) {
        page
        pages
        hasNext
        hasPrev
        objects{
            id
            images
            sold
            name
            basePrice
            category{
                name
            }
            seller {
                name
            }
        }
    }
}