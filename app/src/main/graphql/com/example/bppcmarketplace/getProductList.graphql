query getProductList($page:Int){
    products(page: $page) {
        page
        pages
        hasNext
        hasPrev
        objects{
            name
            basePrice
            category{
                name
            }
            seller {
                name
            }
        }
    }
}